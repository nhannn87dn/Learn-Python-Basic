# Lấy dữ liệu cho biến x = giá trị nhập vào
# từ người dùng.
#x = input('Nhập vào giá trị của x: ')
#print('x', x)
#Đặt một biến name = giá trị của người dùng nhập
# vào, sau đó in ra name

# Cho 2 biến x, y = giá trị nhập từ người dùng
# Yêu cầu: Tính tổng của x và y, sau đó in ra.
# Ví dụ: x = 5, y = 2 ==> mong muồn kq = 7
x = input('Nhập vào x: ')
y = input('Nhập vào y: ')
print(type(x), type(y))
print(int(x) + int(y))
# Kết luận: Giá trị nhập vào từ người dùng
# có kiểu dữ liệu là chuỗi (str)