Viết một hàm có tên calculate_sum nhận vào hai tham số và trả về tổng của hai số đó.

Viết một hàm có tên is_prime nhận vào một số nguyên dương và trả về True nếu số đó là số nguyên tố, ngược lại trả về False.

Viết một hàm có tên calculate_factorial nhận vào một số nguyên dương và trả về giai thừa của số đó.

Viết một hàm có tên reverse_string nhận vào một chuỗi và trả về chuỗi đảo ngược của chuỗi đó.

Viết một hàm có tên find_max nhận vào một danh sách các số và trả về số lớn nhất trong danh sách đó.

Viết một hàm có tên calculate_area_of_circle nhận vào bán kính của hình tròn và trả về diện tích của hình tròn đó.

Viết một hàm có tên is_palindrome nhận vào một chuỗi và trả về True nếu chuỗi đó là palindrome, ngược lại trả về False.

Viết một hàm có tên calculate_power nhận vào hai số nguyên x và n, và trả về giá trị của x^n (x mũ n).

Viết một hàm có tên count_vowels nhận vào một chuỗi và trả về số lượng nguyên âm trong chuỗi.

Viết một hàm có tên generate_fibonacci nhận vào một số nguyên dương n và trả về các số Fibonacci từ 1 đến n dưới dạng một danh sách.